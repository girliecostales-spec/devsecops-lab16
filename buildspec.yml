version: 0.2

phases:
  install:
    commands:
      - echo "Installing security scanning tool"
      - pip3 install checkov
  build:
    commands:
      - echo "Running security scan"
      - checkov -d . --quiet || echo "Security scan completed with findings"
      - echo "Build completed"

artifacts:
  files:
    - '**/*'
